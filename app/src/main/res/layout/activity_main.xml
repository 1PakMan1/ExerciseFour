<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/left_step"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_left_foot"
            android:visibility="invisible" />

        <ImageView
            android:id="@+id/right_step"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_right_foot"
            android:visibility="invisible" />
    </LinearLayout>

    <Button
        android:id="@+id/start_walking_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/start_walking" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/plus_speed_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="45dp"
        android:layout_marginBottom="90dp"
        android:clickable="true"
        app:srcCompat="@drawable/ic_plus" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/minus_speed_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="45dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="90dp"
        android:clickable="true"
        app:srcCompat="@drawable/ic_minus" />

</RelativeLayout>